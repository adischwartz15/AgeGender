# Optional extra for the supplementary ImageNet-pretrained transfer-learning
# experiments only (src/models/pretrained_volo.py, src/models/pretrained_resnet.py,
# scripts/run_transfer_learning.py). Not required to install, import, or run
# any core (from-scratch) experiment -- see README.md "Core vs. supplementary"
# section.
#
#     pip install -r requirements.txt
#     pip install -r requirements-transfer.txt   # only if you want to run the VOLO or pretrained-ResNet extensions
timm>=1.0,<2.0
torchvision>=0.17,<1.0
