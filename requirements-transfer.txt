# Optional extra for the non-parametric "frozen_backbone" baseline
# (src/models/pretrained_resnet.py, src/evaluation/nonparametric/features.py),
# which uses a frozen ImageNet-pretrained torchvision ResNet as a feature
# extractor. Not required to install, import, or run any core (from-scratch)
# experiment.
#
#     pip install -r requirements.txt
#     pip install -r requirements-transfer.txt   # only if you want the frozen_backbone baseline
torchvision>=0.17,<1.0
